<%= turbo_stream.replace dom_id(@todo_list) do %>
  <%= render partial: "todo_lists/todo_list",
             locals: { todo_list: @todo_list, selected_list: @todo_list } %>
<% end %>

<%= turbo_stream.update "main_content" do %>
  <%= render "main_content", todo_list: @todo_list %>
<% end %>
